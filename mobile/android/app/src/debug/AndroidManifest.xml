<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Local development uses the host backend through http://10.0.2.2:8000.
         Release builds retain Android's default HTTPS-only policy. -->
    <application android:usesCleartextTraffic="true"/>
</manifest>
